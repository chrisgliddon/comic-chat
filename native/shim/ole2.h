// ole2.h stand-in - icchat.h includes it after windows.h. OLE is dropped from the
// native build; this exists so the include resolves.
#ifndef NATIVE_SHIM_OLE2_H
#define NATIVE_SHIM_OLE2_H
#include "win32types.h"
#endif
